# گزارش بررسی کد — Neon Anime (نسخه‌ی zip شماره ۴)

> تاریخ بررسی: ۱۷ شهریور ۱۴۰۵ (2026-09-08)
> روش بررسی: خواندن کامل ۴ صفحه HTML + `style.css` + `script.js`، بررسی ۵۱ تصویر، و **اجرای واقعی صفحات در یک DOM شبیه‌سازی‌شده (jsdom)** برای تست کلیک‌ها، فرم‌ها و خطاهای جاوااسکریپت.

---

## ۰. خلاصه‌ی یک‌خطی

پروژه یک **پروتوتایپ فرانت‌اند بسیار خوش‌ظاهر** است، نه یک محصول. هیچ بک‌اندی وجود ندارد، همه‌چیز (لاگین، VIP، لیست تماشا، پیشرفت تماشا) با `localStorage` شبیه‌سازی شده و تقریباً همه‌ی داده‌ها (عنوان، امتیاز، قسمت‌ها، نظرات) به‌صورت دستی داخل HTML کپی شده‌اند. برای «گذاشتن روی هاست به‌عنوان دمو» با چند اصلاح فوری آماده می‌شود؛ برای «راه‌اندازی به‌عنوان سرویس واقعی» هنوز چیزی جز لایه‌ی UI ساخته نشده.

| بخش | وضعیت |
|---|---|
| طراحی بصری / UI | ★★★★☆ — واقعاً خوب؛ توکن‌های رنگ، گلس‌مورفیسم، RTL، موبایل |
| صحت کد HTML/JS | ★★☆☆☆ — چند باگ جدی، به‌خصوص `watch.html` و `script.js` |
| مسیر کاربر (User Flow) | ★☆☆☆☆ — دکمه‌ی «پخش» به هیچ‌جا نمی‌رود؛ صفحه‌ی پلیر یتیم است |
| پرفورمنس | ★☆☆☆☆ — حدود ۶.۷ مگابایت تصویر فقط برای صفحه‌ی اصلی |
| آمادگی برای سرور واقعی | ☆☆☆☆☆ — بک‌اند، احراز هویت، ویدیو، پرداخت: هیچ‌کدام وجود ندارد |

---

## ۱. چی داریم؟

```
index.html    1313 خط   صفحه‌ی اصلی (هیرو اسلایدر، ردیف کارت‌ها، تقویم، Top10، FAQ، خبرنامه، مودال جزئیات)
login.html     413 خط   ورود / ثبت‌نام (انتخاب جنسیت و آواتار)
profile.html   692 خط   داشبورد کاربر
watch.html     515 خط   پلیر ویدیو سفارشی + لیست قسمت‌ها + نظرات
style.css     1762 خط   ۶۷۲ بلاک استایل، ۱۰ media query، ۱۹ تا !important
script.js      580 خط   منطق مشترک (منو، مودال‌ها، اسلایدر، توست، ...)
image-search/  51 فایل  ۹.۲ مگابایت
```

**وابستگی‌های خارجی (CDN):** Google Fonts (Vazirmatn + Orbitron)، typed.js از jsdelivr، GSAP از cdnjs، و در `watch.html` فونت Vazirmatn از یک CDN دیگر (jsdelivr/github).

**نکات مثبت که ارزش حفظ‌کردن دارند:**
- سیستم رنگ با CSS Variable (`--accent`, `--bg-main`, ...) یکدست است.
- بدون فریم‌ورک؛ روی هر هاست استاتیکی بالا می‌آید.
- منوی کشویی موبایل + Bottom Navigation + مگامنو دسکتاپ خوب پیاده شده.
- اسکلت پلیر سفارشی (Play/Pause، Mute، Fullscreen، Skip Intro، Progress) کار می‌کند.
- Reveal-on-scroll با IntersectionObserver، توست، FAQ آکاردئونی: همه سالم‌اند.

---

## ۲. باگ‌های واقعی (با مدرک)

### 🔴 B1. `watch.html` خراب است — کاراکترهای `\n` به‌صورت متن داخل فایل ذخیره شده‌اند
خط ۱ فایل، کل `<head>` را در **یک خط** با رشته‌ی `\n` (بک‌اسلش + n، نه خط جدید واقعی) دارد:

```
<!DOCTYPE html>\n<html lang="fa" dir="rtl">\n<head>\n    <meta charset="UTF-8">\n ...
```

همین الگو در خط‌های ۱۲۸، ۱۴۸–۱۴۹ و ۴۱۰ هم تکرار شده. نتیجه در مرورگر:
- چون بین `<html>` و `<head>` متنِ غیرخالی هست، پارسر مرورگر `<head>` را نادیده می‌گیرد و `meta`/`link`/`title` همگی داخل `<body>` می‌افتند (در تست: `head.children.length === 0`).
- **۱۰ رشته‌ی `\n` به‌صورت متن روی صفحه رندر می‌شوند.**
- ۳ تگ `</div>` اضافه هم در فایل هست (`<div>`=108 و `</div>`=111).

این ظاهراً نتیجه‌ی یک بار paste شدن خروجی JSON/escape‌شده به داخل فایل است. باید فایل بازنویسی/تمیز شود.

### 🔴 B2. دکمه‌های «پخش» به هیچ‌جا نمی‌روند و `watch.html` از هیچ صفحه‌ای لینک نشده
- در کل پروژه **حتی یک** `href="watch.html"` وجود ندارد.
- `script.js` خط ۱۳۹ به `#playerModal` اشاره می‌کند که در هیچ HTMLای وجود ندارد؛ پس هندلر ۲۴ دکمه‌ی `.play-trigger` در `index.html` وارد `if(playerModal)` نمی‌شود و **هیچ کاری نمی‌کند** (تست: کلیک → نه ناوبری، نه توست).
- یعنی مهم‌ترین مسیر محصول (کلیک روی پخش → دیدن ویدیو) عملاً قطع است.

### 🔴 B3. `watch.html` اجزای مشترک را ندارد و در نتیجه خطای JS می‌دهد
در `watch.html` این‌ها وجود ندارند: `#sideMenu`، `#mobileBackdrop`، `#closeSideBtn`، `#toastContainer`، `#vipModal`، و اسکریپت اتصال `bottomNavSearch/VIP`.
پیامدها (همه در تست تأیید شد):
- کلیک روی **هر** لینک `href="#"` (۱۹ تا در این صفحه، از جمله کل منوی بالا) → `TypeError: Cannot read properties of null (reading 'classList')` در `script.js` خط ۳۸۲.
- همبرگر موبایل هیچ کاری نمی‌کند.
- «جستجو» و «ویژه» در نوار پایین موبایل کار نمی‌کنند.
- هیچ توستی نمایش داده نمی‌شود (مثلاً «افزودن به لیست» بازخورد ندارد).

### 🟠 B4. ساختار `script.js` به‌هم‌ریخته است
ردیابی عمق آکولادها نشان می‌دهد:
- `DOMContentLoaded` در **خط ۴۷۵** بسته می‌شود؛ بخش‌های ۱۷ (رادیو)، ۱۸ (Drag-to-scroll) و ۱۹ (مودال جزئیات) **بیرون** آن هستند.
- بخش ۱۸ (Drag-to-scroll) اشتباهاً **داخل** هندلر کلیک `radioPlayBtn` تو در تو شده (خط ۵۰۹). چون `#radioWidget` در هیچ صفحه‌ای وجود ندارد، شرط `if(radioVisualizer && radioWidget)` هیچ‌وقت true نمی‌شود → **Drag-to-scroll هرگز فعال نمی‌شود.**
- `showToast` در خط ۴۸۵ خارج از scope تعریفش صدا زده شده؛ اگر روزی رادیو اضافه شود `ReferenceError` می‌دهد.
- بخش ۱۹ فقط به این دلیل کار می‌کند که `script.js` انتهای `<body>` لود می‌شود.
- CSS مرده: `.radio-widget` (۶ قانون)، `.player-modal` (۳)، `.glass-card-title` — برای المان‌هایی که در HTML نیستند.

### 🟠 B5. Mixed Content — ۹ ویدیو با `http://`
`index.html` (۸ مورد) و `watch.html` (خط ۱۵۵) به `http://commondatastorage.googleapis.com/.../TearsOfSteel.mp4` اشاره می‌کنند. روی سرور HTTPS، مرورگرها این را یا مسدود یا اجباراً upgrade می‌کنند (رفتار بین Chrome/Firefox/Safari یکسان نیست). حداقل باید `https://` شود — و در محصول واقعی اصلاً نباید به باکت نمونه‌ی گوگل وابسته بود.

همچنین ۷ تگ `<video class="card-hover-video">` بدون `preload="none"` هستند → مرورگر ممکن است در لود صفحه برای همه‌شان درخواست بفرستد.

### 🟠 B6. تصاویر: ۹.۲ مگابایت، نیمی بلااستفاده، ابعاد غیرمنطقی
- **۲۵ فایل از ۵۱ فایل هیچ‌جا استفاده نشده** (~۲.۶ مگابایت وزن مرده).
- تصاویرِ استفاده‌شده در صفحه‌ی اصلی ≈ **۶.۷ مگابایت** (بدون `loading="lazy"`، بدون `srcset`).
- نمونه‌ها:
  - `anime-aesthetic-avatar-square-1.jpg` — **۳۸۴۰×۶۸۲۶ پیکسل، ۱.۱ مگابایت** — به‌عنوان آواتار ۴۰ پیکسلی نوبار!
  - `attack-on-titan-final-season-desktop-wal-1.jpg` — ۳۸۴۰×۲۱۶۰، ۱.۴ مگابایت — در ۱۲ جای مختلف تکرار شده.
  - `mikasa-ackerman-solo-profile-picture-hd-1.jpg` — ۴۷۲۴×۳۰۰۰، ۶۱۶ کیلوبایت — آواتار.
- روی اینترنت موبایل ایران این یعنی چندین ثانیه صفحه‌ی سفید (و `body { opacity:0 }` اولیه هم این حس را بدتر می‌کند).

### 🟡 B7. UIهایی که فقط ظاهرشان هست (هیچ هندلری ندارند)
تست شد؛ کلیک روی این‌ها هیچ اتفاقی رقم نمی‌زند:
- چیپ‌های فیلتر «همه / اکشن / درام / تخیلی / سینمایی»
- تب‌های مودال جزئیات («پیش‌نمایش‌ها»، «عوامل»)
- Enter در باکس جستجو و «جستجوهای پرطرفدار»
- دکمه‌های «انتخاب پلن / خرید اشتراک ویژه» در مودال VIP (۳ صفحه)
- «ویرایش پروفایل»، «ارسال نظر»، لایک/لیست/اشتراک در `watch.html`
- کلیک روی قسمت‌ها در سایدبار پلیر (`.ep-card`) — قسمت عوض نمی‌شود
- کارت‌های دسته‌بندی، دکمه‌ی «⌄ اطلاعات» روی کارت‌ها، لینک «رمز عبور را فراموش کرده‌اید؟»
- منوی سه‌نقطه و مگامنو: همه‌ی آیتم‌ها `href="#"` → توست «در نسخه دمو غیرفعال است» (۳۰ لینک در index، ۳۰ در profile، ۱۹ در watch)

### 🟡 B8. مشکلات کوچک‌تر ولی واقعی
- **خروج از حساب در موبایل ممکن نیست:** تنها لینک «خروج» داخل `.profile-sidebar` است که در `max-width:900px` مخفی می‌شود (`profile.html` خط ۱۳۵ و ۴۶۷).
- خروج فقط `neon_is_logged_in=false` می‌کند؛ نام/آواتار/VIP در `localStorage` می‌ماند و در لاگین بعدی همان کاربر قبلی برمی‌گردد (`login.html` خط ۳۴۰: «Demo logic»).
- ورود با **هر** ایمیل/رمزی موفق است و همیشه VIP می‌دهد؛ ثبت‌نام ایمیل و رمز را اصلاً ذخیره/ارسال نمی‌کند (طبیعتاً، چون بک‌اند نیست).
- `style="cursor:pointer;" style="cursor:pointer;"` — اتریبیوت تکراری روی `.audio-visualizer` در ۳ فایل.
- `index.html` خط ۱۳۰۱ به `#loginTriggerBtn` اشاره می‌کند که وجود ندارد (بی‌خطر ولی کد مرده).
- `profile.html` بلاک CSS مربوط به `.profile-cards-row` **دو بار** paste شده و بار دوم یک `@media` داخل `@media` دیگر گیر افتاده (خط ۱۷۷–۲۱۰).
- ارقام مخلوط فارسی/لاتین: «۹.4»، «۹.10» (که یعنی «نه و ده!»)، «۹.۱». امتیاز یک انیمه در بخش‌های مختلف فرق دارد (جوجوتسو کایسن: ۹.۸ / ۹.۴ / ۹.۲؛ شیطان‌کش: ۹.۷ / ۹.۱ / ۹.۱۰). نام‌ها هم یکدست نیست («اتک آن تایتان» / «حمله به تایتان»). ریشه: نبود منبع داده‌ی واحد.
- «دوشنبه (امروز)»، تاریخ اخبار، «۱۲,۴۵۰ کاربر آنلاین»، نظرات کاربران — همه هاردکد و ساختگی‌اند. قبل از انتشار عمومی یا واقعی شوند یا حذف.
- هیچ صفحه‌ای `<footer>` ندارد. `index.html` هیچ `<h1>` ندارد.
- هیچ `favicon`، `meta description`، Open Graph، `robots.txt`، `sitemap.xml`، صفحه‌ی 404 وجود ندارد.
- دسترس‌پذیری: صفر `aria-*`، دکمه‌های ایموجی بدون label، هیچ مودالی با Escape بسته نمی‌شود، Focus trap ندارد.
- ۵۳ / ۵۲ / ۸۲ استایل inline در سه صفحه‌ی اصلی؛ نوبار + منو + مودال جستجو + مودال VIP + نوار پایین در ۳ فایل دستی کپی شده‌اند و **همین الان از هم فاصله گرفته‌اند** (watch.html ناقص است). با هر تغییر باید ۳ جا را ویرایش کرد.

---

## ۳. نکات امنیتی (برای وقتی که بک‌اند می‌آید)

1. **VIP سمت کلاینت بی‌معنی است.** هرکس در DevTools بزند `localStorage.neon_is_vip = 'true'` «کاربر ویژه» می‌شود. قفل قسمت‌ها (قسمت ۲۲ در watch.html) باید سمت سرور اعمال شود و لینک ویدیو اصلاً برای غیر‌VIP تولید نشود (Signed URL با انقضا).
2. **رندر با `innerHTML`** (`renderSchedule` در `script.js` خط ۱۰۹) امروز بی‌خطر است چون داده ثابت است؛ به‌محض اینکه عنوان‌ها/نظرات از دیتابیس یا کاربر بیایند، XSS است. یا `textContent` یا یک تابع escape.
3. رمز عبور نباید هیچ‌وقت در `localStorage` ذخیره شود (الان نمی‌شود، خوب است — فقط توکن سشن/JWT کوتاه‌عمر و ترجیحاً کوکی HttpOnly).
4. سرور باید HSTS، CSP (حداقل `default-src 'self'` + CDNهای مشخص)، `X-Frame-Options`، Rate-limit روی login/OTP داشته باشد.
5. **حقوقی:** تصاویر از جستجوی تصویر برداشته شده‌اند (پوسترها و والپیپرهای دارای حق نشر) و خود پخش انیمه بدون لایسنس ریسک حقوقی دارد. برای انتشار عمومی باید فکری برایش شود (حداقل صفحه‌ی قوانین/حریم خصوصی/DMCA).

---

## ۴. برای «بردن روی سرور» دقیقاً چی کم است؟

### فاز A — حداقل‌های لازم تا همین دمو را بشود روی هاست گذاشت (۱–۲ روز کار)
- [ ] بازنویسی تمیز `watch.html` (حذف `\n`های متنی، `</div>`های اضافه، اضافه‌کردن sideMenu/backdrop/toastContainer/vipModal و اسکریپت bottom-nav)
- [ ] وصل‌کردن `.play-trigger`ها به `watch.html` (فعلاً حتی با پارامتر ساده مثل `watch.html?anime=aot&ep=21`)
- [ ] `http://` → `https://` برای ۹ ویدیو، و `preload="none"` روی ویدیوهای hover
- [ ] اصلاح ساختار `script.js` (بخش‌های ۱۷/۱۸/۱۹ به داخل `DOMContentLoaded`، جداکردن Drag-to-scroll از هندلر رادیو، حذف/پیاده‌سازی رادیو)
- [ ] بهینه‌سازی تصاویر: حذف ۲۵ فایل بلااستفاده، تبدیل به WebP، ریسایز (آواتار ≤ ۲۵۶px، والپیپر ≤ ۱۹۲۰px)، `loading="lazy"` برای زیر خط تا، `width/height` روی `<img>`
- [ ] یک‌دست‌کردن ارقام و امتیازها؛ حذف یا واقعی‌کردن شمارنده‌ی «آنلاین» و نظرات جعلی
- [ ] `favicon`، `<meta name="description">`، OG tags، `<h1>`، فوتر، صفحه‌ی 404
- [ ] لینک «خروج» برای موبایل (در منوی کشویی) + پاک‌کردن کامل کلیدهای `neon_*` هنگام خروج
- [ ] Self-host کردن فونت‌ها و GSAP/typed.js (Google Fonts از ایران کند/ناپایدار است) و minify کردن CSS/JS
- [ ] خارج‌کردن استایل‌های inline و بلاک‌های تکراری CSS در `profile.html`

### فاز B — تبدیل به محصول واقعی (این‌ها اصلاً وجود ندارند)
| نیاز | توضیح |
|---|---|
| **بک‌اند + دیتابیس** | مدل‌های `users`, `anime`, `seasons`, `episodes`, `genres`, `watchlist`, `watch_progress`, `comments`, `subscriptions`, `payments`, `notifications`. پیشنهاد: PostgreSQL + یکی از Laravel / Django / NestJS (هر سه در ایران رایج‌اند). |
| **احراز هویت** | ثبت‌نام/ورود واقعی با هش رمز (bcrypt/argon2)، OTP پیامکی (کاوه‌نگار، ملی‌پیامک، ...)، فراموشی رمز، سشن یا JWT + refresh token، محدودیت تعداد دستگاه هم‌زمان (که در پلن‌ها وعده داده شده). |
| **API** | حداقل: `GET /anime`, `GET /anime/:slug`, `GET /episodes/:id/stream`, `POST /auth/*`, `GET/POST /me/watchlist`, `PUT /me/progress`, `GET/POST /episodes/:id/comments`, `GET /search?q=`, `GET /schedule`. فرانت باید کارت‌ها را از JSON رندر کند، نه HTML دستی. |
| **زیرساخت ویدیو** | نمی‌شود mp4 را مستقیم از سرور داد. نیاز به ترنسکد به HLS با چند کیفیت (ffmpeg)، CDN، Signed URL با انقضا، و پلیر HLS (hls.js) روی همین UI. گزینه‌ی داخلی: سرویس VOD آروان‌کلود. زیرنویس (WebVTT) و انتخاب صدا (دوبله/زیرنویس) که در UI وعده داده شده. |
| **پرداخت** | اتصال پلن‌های ۹۹/۲۵۰/۸۵۰ هزار تومانی به درگاه (زرین‌پال، آیدی‌پی، زیبال)، callback و تأیید سمت سرور، تاریخ انقضای اشتراک، فاکتور. |
| **پنل ادمین / CMS** | افزودن انیمه، فصل، قسمت، آپلود ویدیو، برنامه‌ی پخش هفتگی، اخبار، مدیریت نظرات و کاربران. بدون این، هر قسمت جدید یعنی ویرایش HTML. |
| **جستجو و فیلتر** | حداقل full-text دیتابیس؛ بهتر: Meilisearch (پشتیبانی خوب فارسی). صفحه‌ی `catalog.html` که در README وعده داده شده. |
| **اعلان‌ها و خبرنامه** | فرم خبرنامه الان فقط انیمیشن است. نیاز به سرویس ایمیل/پیامک + جدول subscribers + Web Push (اختیاری). |
| **DevOps** | HTTPS (Let's Encrypt)، Nginx با gzip/brotli و cache header برای استاتیک‌ها، متغیرهای محیطی، بکاپ خودکار دیتابیس، لاگ و مانیتورینگ (Sentry/Uptime)، CI برای دیپلوی. |
| **صفحات حقوقی** | قوانین استفاده، حریم خصوصی، تماس با ما، درباره‌ی ما — هم برای کاربر، هم برای درگاه پرداخت که اینها را الزامی می‌کند. |

### فاز C — بازسازی معماری فرانت (پیشنهادی، قبل از فاز B بهتر است)
- یک لایه‌ی مشترک برای نوبار/منو/مودال‌ها (یا با یک SSG/تمپلیت مثل Eleventy/Nunjucks، یا فریم‌ورک مثل Next.js/Nuxt، یا حداقل یک `layout.js` که این بخش‌ها را تزریق کند) تا ۳ نسخه‌ی کپی‌شده یکی شوند.
- **منبع داده‌ی واحد:** یک `data/anime.json` (و بعداً API) که کارت‌ها، Top10، تقویم، مودال جزئیات و صفحه‌ی پلیر همه از آن پر شوند. با این کار امتیازها/نام‌ها هم خودبه‌خود یکدست می‌شوند.
- شکستن `style.css` به ماژول‌ها (`base`, `nav`, `cards`, `player`, `auth`, `profile`) و حذف ۱۹ تا `!important`.
- یک مرحله‌ی build سبک (Vite) برای minify، hash کردن فایل‌ها و تبدیل تصاویر.
- بعد از آن: PWA (manifest + service worker) که در README به‌عنوان قدم بعدی آمده — ولی فعلاً اولویت ندارد.

---

## ۵. اولویت‌بندی پیشنهادی

1. **همین هفته:** فاز A (به‌خصوص B1، B2، B3، B5، B6). بدون این‌ها حتی دمو هم روی سرور «خراب» دیده می‌شود.
2. **قبل از نوشتن هر خط بک‌اند:** منبع داده‌ی واحد + لایه‌ی مشترک (فاز C، دو مورد اول). وگرنه بک‌اند را باید به ۳ نسخه‌ی متفاوت از UI وصل کنید.
3. **بعد:** فاز B به ترتیب: احراز هویت → API کاتالوگ → پلیر HLS → پرداخت → ادمین.

---

## ۶. جمع‌بندی

کد از نظر «حس و ظاهر» بالاتر از حد یک پروتوتایپ معمولی است و ارزش نگه‌داشتن دارد. اما از نظر مهندسی، الان یک **ماکاپ تعاملی** است: چند باگ ساختاری (فایل `watch.html` خراب، پلیر یتیم، `script.js` به‌هم‌ریخته)، وزن غیرقابل‌قبول تصاویر، و صفر درصد بک‌اند. با یکی‌دو روز کار فاز A می‌شود دموی سالمی روی هاست گذاشت؛ برای «سرویس واقعی» باید کل لایه‌ی سرور از صفر طراحی شود و فرانت هم قبل از آن به یک منبع داده‌ی واحد متصل شود.
