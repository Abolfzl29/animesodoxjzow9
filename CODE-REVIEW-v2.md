# گزارش بررسی کد — نسخه ۲ (پروژه ۱۹ صفحه‌ای)

> **وضعیت اصلاحات (به‌روزرسانی ۲۴ شهریور ۱۴۰۵):** همه‌ی موارد بخش‌های ۲ و ۳ که با فرانت‌اند قابل حل بودند اصلاح شدند (کامیت «Fix all review findings» + اصلاحات تکمیلی بعدی):
> باگ‌های ب1 تا ب6 ✅ — خودمیزبانی GSAP/typed.js ✅ — هدرهای امنیتی و کش اصلاح‌شده در nginx ✅ — بامپ کشِ service worker و تکمیل precache ✅ — یکسان‌سازی تقویم‌ها در `data/schedule.js` ✅ — تست خودکار + CI (`npm test` / `.github/workflows/ci.yml`) ✅.
> **همچنان باز (نیازمند بک‌اند/محتوا):** احراز هویت و ذخیره‌سازی سمت سرور، اعتبارسنجی VIP روی سرور، ویدیوی واقعی، پنل مدیریت محتوا.

> تاریخ بررسی: ۱۵ سپتامبر ۲۰۲۶ (۲۴ شهریور ۱۴۰۵)
> روش بررسی: بارگذاری **هر ۱۸ صفحه‌ی HTML در مرورگر شبیه‌سازی‌شده (jsdom)**، اجرای کامل همه‌ی اسکریپت‌ها، و شبیه‌سازی جریان‌های واقعی کاربر (کلیک پخش، ورود/ثبت‌نام، خرید پلن، ثبت نظر با کد مخرب، جست‌وجو، صفحه‌های کتابخانه) — جمعاً ۱۶ تست تعاملی + بررسی استاتیک تک‌تک فایل‌ها.

> **وضعیت اصلاحات (به‌روزرسانی همان روز):** همه‌ی باگ‌های فرانت‌اندی بخش ۲ و موارد سخت‌افزاری/امنیتی بخش ۳ رفع شدند:
>
> | مورد | وضعیت |
> |---|---|
> | ب1 — نمایش انیمه‌ی اشتباه با لینک خراب در `watch.html` | ✅ رفع شد — پنل «پیدا نشد» مثل `anime.html` |
> | ب2 — متن برعکس حالت خالی حس‌وحال | ✅ رفع شد («نداریم») |
> | ب3 — تاریخ «همین الان» همیشگی نظرات | ✅ رفع شد — ذخیره‌ی `time` و نمایش نسبی/تاریخ شمسی |
> | ب4 — دو تقویم پخش متناقض | ✅ رفع شد — منبع واحد `data/schedule.js` برای خانه و صفحه تقویم + تب «امروز» داینامیک |
> | ب5 — ورود به همه VIP می‌داد | ✅ رفع شد — حفظ وضعیت VIP موجود؛ `logout` هم دیگر VIP را نمی‌پرد |
> | ب6 — شکنندگی‌ها (`lastChild`، بدون چک `null`) | ✅ رفع شد — `span` اختصاصی + گاردها |
> | وابستگی CDN (GSAP/typed.js) | ✅ رفع شد — خودمیزبان در `assets/vendor/` |
> | `sw.js` (نسخه‌ی کش، صفحات جامانده) | ✅ رفع شد — `neon-anime-v2` + precache کامل |
> | هدرهای امنیتی و کش `immutable` در nginx | ✅ رفع شد — CSP/nosniff/Referrer-Policy/Permissions-Policy + کش بدون `immutable` + بازنویسی `sitemap`/`robots` با هاست واقعی |
> | متای توضیحات `legal.html` | ✅ رفع شد |
> | منسوخ‌بودن `CODE-REVIEW.md` | ✅ برچسب خورد و به این سند ارجاع می‌دهد |
> | نبود تست/CI | ✅ رفع شد — `tests/run-tests.js` + `npm test` + گیت‌هاب‌اکشنز |
>
> **همچنان باز (نیازمند بک‌اند/زیرساخت):** احراز هویت و ذخیره‌سازی سمت سرور، ویدیوی واقعی، درگاه پرداخت، و شمارنده‌ی آنلاینِ واقعی — همان موارد بخش ۴ (P0) که با کامنت‌های `TODO(backend)` مشخص‌اند.

---

## ۰. حکم کلی

پروژه از یک پروتوتایپ ۴ صفحه‌ای به یک **اپلیکیشن فرانت‌اند ۱۹ صفحه‌ای (~۷٬۸۰۰ خط جاوااسکریپت، ~۷٬۲۰۰ خط HTML، ~۷٬۰۰۰ خط CSS)** تبدیل شده و کیفیت کد نسبت به بررسی قبلی جهش بزرگی داشته است:

| معیار | نتیجه‌ی تست واقعی |
|---|---|
| خطای runtime در لود هر ۱۸ صفحه | **صفر** |
| فایل گم‌شده (img/src/href محلی) | **صفر** |
| محتوای مخلوط (http://) | **صفر** |
| عبور کد مخرب (XSS) از نظرات/جست‌وجو | **صفر** (تست شد) |
| تست‌های تعاملی جریان‌های کاربر | **۱۶ از ۱۶ قبول** |

یعنی از نظر «آیا الان بدون خطا اجرا می‌شود؟» پاسخ **بله** است. مشکلات باقی‌مانده بیشتر از جنس **معماری/محصول** است تا باگ فنی، به‌علاوه چند باگ کوچک واقعی که پایین آمده.

---

## ۱. چه چیزهایی واقعاً خوب است (حفظشان کن)

1. **معماری داده‌ی متمرکز.** `data/anime.js` تنها منبع حقیقت است؛ هیچ عنوان/پوستری در `localStorage` کپی نمی‌شود. سازنده‌های آدرس (`watchUrl`, `detailUrl`, `downloadUrl`, `progressKey`) همه از یک‌جا می‌آیند — به‌همین‌دلیل هیچ لینک مرده‌ای در کل سایت پیدا نشد.
2. **امنیت سمت رندر.** هر جا محتوای کاربری نمایش داده می‌شود یا `escapeHtml` هست یا `textContent` (کامنت‌ها در `mag.js` صریحاً با کامنت «secure» نوشته شده‌اند). تست تزریق `<img src=x onerror=...>` در نظرات `watch.html` بی‌اثر بود. `nextUrl` در `auth.js` هم در برابر `javascript:` و آدرس‌های مطلق مقاوم است (تست شد). هیچ `eval` یا `document.write`ای در پروژه نیست.
3. **قرارداد ذخیره‌سازی مستند.** هدر `library.js` دقیقاً کلیدهای `localStorage` و معنای هر کدام را نوشته و چند فایل (پلیر، کتابخانه، گیمیفیکیشن) واقعاً به همان قرارداد پایبندند. تابع `migrateLegacyHistory` برای داده‌های قدیمی هم وجود دارد.
4. **کد دفاعی.** تقریباً همه‌ی خواندن‌های `localStorage` داخل `try/catch` است، المان‌ها قبل از استفاده چک می‌شوند، و وابستگی‌های CDN (GSAP/typed.js) «اختیاری» هستند — اگر لود نشوند صفحه نمی‌شکند.
5. **پلیر کامل.** یادآوری ادامه‌ی پخش، پرش خودکار به قسمت بعد، قفل VIP روی قسمت‌ها، کلیدهای میان‌بر (space/k/m/f/جهت‌ها)، و سکوی پیشروی آگاه از RTL — همگی درست پیاده‌شده‌اند.
6. **موتور کتابخانه (`library.js`).** مرتب‌سازی چند حالته، حالت مدیریت/حذف گروهی با دیالوگ تأیید، صفحه‌های خالی/خطا/هشدار، و تفکیک درست «ادامه تماشا / تکمیل‌شده / تاریخچه» با آستانه‌های ۱٪ و ۹۵٪.
7. **PWA و استقرار.** `manifest` + service worker با استراتژی درست (ناوبری network-first، استاتیک‌ها stale-while-revalidate)، فونت‌های خودمیزبان با `preload`، Dockerfile/nginx با تزریق `$PORT`، و `railway.json` سالم‌اند.
8. **دسترس‌پذیری بهتر از متوسط.** `aria-expanded`، برگرداندن فوکوس بعد از بستن مودال، `Escape` برای همه‌ی لایه‌ها، `alt` تصاویر، و کارت‌های قسمت با `role="button"` و هندلر کیبورد.
9. **عملکرد تصاویر.** همه‌چیز WebP و جمعاً ~۱.۱ مگابایت، `loading="lazy"` گسترده، ویدیوهای هاور با `preload="none"`.

---

## ۲. باگ‌های واقعی (همه با تست تأیید شدند)

### 🔴 ب1. `watch.html` با شناسه‌ی نامعتبر، انیمه‌ی اشتباه را پخش می‌کند — بدون هیچ پیامی
`watch.html?anime=does-not-exist` به‌جای صفحه‌ی «پیدا نشد» (که `anime.html` دارد) بی‌صدا **اولین انیمه‌ی آرشیو (حمله به تایتان)** را نشان می‌دهد. کاربری که لینک خراب را باز کند متوجه نمی‌شود چیز دیگری می‌بیند.
> محل: بلاک «Resolve which anime» در `watch.html` — fallback به `DATA.list[0]`.

### 🟠 ب2. متن حالت خالی در «حس و حال» برعکس است
`gamification.js` خط ~۶۱۹ وقتی نتیجه‌ای نیست می‌گوید: «فعلاً انیمه‌ای برای این حس و حال **داریم** — به‌زودی اضافه می‌شود!»؛ باید باشد «**نداریم**». فعلاً با داده‌ی موجود به این شاخه نمی‌رسیم، ولی با اولین تغییر مود/ژانر کاربران متن غلط می‌بینند.

### 🟠 ب3. تاریخ نظرات همیشه «همین الان» است
در `watch.html` موقع ذخیره‌ی نظر `when: 'همین الان'` هاردکد شده و اصلاً زمان واقعی ذخیره نمی‌شود. نتیجه: نظری که هفته‌ی پیش ثبت شده هم «همین الان» نمایش داده می‌شود. (در `mag.js` زمان درست ذخیره می‌شود — قرارداد دو صفحه با هم فرق دارد.)

### 🟠 ب4. دو تقویم پخش متناقض
ویجت صفحه‌ی اصلی (`scheduleData` در `script.js`) و صفحه‌ی تقویم (`SCHEDULE` در `schedule.js`) **دو مجموعه‌داده‌ی جداگانه با روزها و ساعت‌های متفاوت‌اند** (مثلاً وان‌پیس در خانه شنبه ۱۴:۳۰، در تقویم شنبه ۲۰:۰۰). یکی باید منبع حقیقت باشد و دیگری از همان بخواند.

### 🟡 ب5. ورود (Login) به همه VIP می‌دهد، ثبت‌نام نه
`login.html` با هر ایمیل/رمزی ورود را می‌پذیرد و `vip: true` می‌دهد؛ ثبت‌نام `vip: false` است. یعنی کاربری که ثبت‌نام کرده و خارج شده، با یک «ورود» ساده همه‌ی قسمت‌های قفل را باز می‌کند. برای دمو قابل‌درک است ولی رفتار غافلگیرکننده‌ای است و باید حداقل در متن دکمه/توست گفته شود.

### 🟡 ب6. آسیب‌پذیری‌های کوچک و شکننده (الان کار می‌کنند، ولی با اولین ویرایش می‌شکنند)
- `watchlistBtn.lastChild.textContent` در `watch.html` به وجود گره متنیِ فاصله وابسته است.
- `applyTheme` در `anime.js` و `download.js` بدون چک کردن `null` مستقیم روی نتیجه‌ی `getElementById('themeToggleBtn')`/`footerYear` متد صدا می‌زند.
- `sw.js`: `CACHE_NAME` از v1 تغییر نکرده و صفحات `anime/watch/article/download/profile` در precache نیستند؛ آفلاین، کاربر بی‌صدا به خانه می‌افتد. کش `immutable` سی‌روزه‌ی nginx روی فایل‌های بی‌هَش هم یعنی تعویض تصویر هم‌نام تا یک ماه دیده نمی‌شود.

---

## ۳. چیزهایی که «دیده نمی‌شوند» ولی مهم‌اند

1. **هیچ بک‌اندی وجود ندارد — و این یعنی:**
   - قفل VIP با یک خط در کنسول باز می‌شود: `localStorage.setItem('neon_is_vip','true')` (در `watch.html` فقط `localStorage` چک می‌شود).
   - نظرات، امتیازها، پیشرفت و لیست‌ها **فقط روی همان دستگاه**اند؛ هیچ کاربری محتوای کاربر دیگر را نمی‌بیند.
   - «فراموشی رمز» و خبرنامه هیچ کاری نمی‌کنند (فقط توست). همه‌ی این نقاط با `TODO(backend)` علامت‌گذاری شده‌اند — خوب است، ولی تا اتصال واقعی محصولی وجود ندارد.
2. **ویدیوها واقعی نیستند.** تمام قسمت‌های کل کاتالوگ فقط ۳ کلیپ دموی گوگل (`TearsOfSteel` و…) را پخش می‌کنند و سایت به باکت خارجی وابسته است.
3. **`CODE-REVIEW.md` قبلی منسوخ است** — مال نسخه‌ی ۴ صفحه‌ای است و وضعیت فعلی را نشان نمی‌دهد.
4. **`robots.txt` و `sitemap.xml` دامنه‌ی GitHub Pages را هاردکد کرده‌اند**؛ اگر روی Railway دیپلوی شود هر دو غلط می‌شوند. همچنین آدرس‌های `?anime=`/`?slug=` در sitemap معمولاً از نظر سئو ارزشی ندارند.
5. **سرور بدون هدرهای امنیتی** است (بدون CSP، `X-Content-Type-Options`، `Referrer-Policy` در `nginx.conf`).
6. **شمارنده‌ی «کاربران آنلاین» عدد تصادفی حول ۱۲٬۴۵۰ می‌سازد** — واقعی نیست. برای دمو اشکالی ندارد ولی در محصول واقعی گمراه‌کننده است.
7. **هیچ تست/CI در ریپازیتوری نیست** (تست‌های این بررسی بیرون از مخزن اجرا شدند؛ می‌توان همان‌ها را به یک GitHub Action تبدیل کرد).
8. **بدون بیلد/مینیفای:** هر صفحه چند فایل سنگین را کامل لود می‌کند (`style.css` ۱۱۳KB، `library.js` ۷۳KB حتی در صفحاتی که فقط یک صفحه‌ی ساده‌اند).
9. **داده‌ها در کد قفل‌اند:** افزودن یک انیمه = ویرایش دستی `data/anime.js`؛ هیچ پنل مدیریتی وجود ندارد.

---

## ۴. نقشه‌ی ارتقا (به ترتیب اولویت)

**P0 — قبل از هر انتشار جدی**
- بک‌اند حداقلی: احراز هویت واقعی (نقاط `TODO(backend)` آماده‌اند)، ذخیره‌ی نظرات/لیست‌ها سمت سرور، و اعتبارسنجی VIP روی سرور.
- ویدیوی واقعی (HLS/استریم) به‌جای باکت دموی گوگل.
- رفع باگ‌های ب1، ب2، ب3 و یکی‌کردن دو تقویم (ب4).

**P1 — زود و ارزان**
- بامپ `CACHE_NAME` و تکمیل precache در `sw.js`؛ کش کوتاه‌تر یا نام‌گذاری با هش برای `/assets/`؛ افزودن هدرهای امنیتی به `nginx.conf`.
- خودمیزبان‌کردن GSAP/typed.js (حذف ۲ وابستگی CDN باقی‌مانده و تکمیل آفلاین واقعی).
- همسان‌سازی آدرس `sitemap`/`robots` با دامنه‌ی استقرار (یا تولید خودکارشان).

**P2 — میان‌مدت**
- ابزار بیلد (Vite): مینیفای، تفکیک کد، هَش‌دارکردن فایل‌ها.
- تبدیل همین تست‌های تعاملی به CI (هر ۱۸ صفحه + ۱۶ سناریو در هر پول‌ریکوئست).
- خبرنامه و «به‌زودی»ها را یا وصل کن یا از منو بردار.

**P3 — بلندمدت**
- تبدیل `data/anime.js` به JSON + fetch (یا CMS/پنل ادمین)، جست‌وجوی واقعی، و آنالیتیکس.

---

## پیوست: روش تست (قابل تکرار)
- لود هر ۱۸ صفحه با `jsdom` + اجرای همه‌ی اسکریپت‌های محلی؛ گزارش: خطای اجرا، فایل گم‌شده، محتوای مخلوط، آید‌ی تکراری، تگ‌های سئو.
- ۱۶ تست تعاملی شامل: پخش→صفحه‌ی پلیر، جست‌وجو، خرید پلن بدون لاگین، فرم ورود، رندر قسمت‌ها، تزریق XSS در نظر، صفحه‌ی «پیدا نشد»، کتابخانه/علاقه‌مندی/ادامه‌ی تماشا با داده‌ی واقعی `localStorage`، و رفتار `logout` (پاک‌کردن نشست و نگه‌داشتن تم).
- نتیجه: **۱۶/۱۶ قبول، صفر خطای اجرا، صفر فایل گم‌شده.**
